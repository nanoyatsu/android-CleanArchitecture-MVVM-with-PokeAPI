query FetchPokedex($offset:Int!,$limit:Int!){
    pokemon_v2_pokemon(
        offset: $offset
        limit: $limit,
        distinct_on: [],
        order_by: [],
        where: {}
    ) {
        id,
        name
    }
}